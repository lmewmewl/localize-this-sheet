name: l10n_this_sheet
description: Sync Flutter ARB localization files with Google Sheets.
version: 1.0.0
repository: https://github.com/your-username/l10n_this_sheet

environment:
  sdk: ">=3.0.0 <4.0.0"

executables:
  push: push
  pull: pull

dependencies:
  googleapis: ^16.0.0
  googleapis_auth: ^2.3.2
  http: ^1.2.0
  dotenv: ^4.2.0

dev_dependencies:
  lints: ^6.1.0
  mocktail: ^1.0.5
  test: ^1.25.0
